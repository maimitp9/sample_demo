<% if @user.errors.any? %>
  <div class="error_explaination">
    <div class="alert alert-error">
      The form contsils <%= pluralize(@user.errors.count, 'error') %>
    </div>
    <ul>
      <% @user.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
